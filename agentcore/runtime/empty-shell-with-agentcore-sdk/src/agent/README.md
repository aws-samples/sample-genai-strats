# Agent

Minimal AgentCore runtime agent built with the `bedrock-agentcore` Python SDK.

## Entrypoint

- `app_entrypoint` — decorated with `@app.entrypoint`, echoes received headers and JSON payload

## Run locally

```bash
uv run main.py
```
